{
  "name": "pkgversion",
  "version": "0.0.1",
  "description": "Returns the current node package version",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "root-require": "^0.3.1"
  },
  "bin": {
    "npv": "npv.js"
  }
}
